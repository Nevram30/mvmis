# Railway Environment Variables Template
# Copy these to your Railway service environment variables

# Disable shadow database completely
SHADOW_DATABASE_URL=""

# Skip Prisma migration generation and seeding
PRISMA_MIGRATE_SKIP_GENERATE=true
PRISMA_MIGRATE_SKIP_SEED=true

# Production environment
NODE_ENV=production

# Your actual DATABASE_URL should already be set by Railway PostgreSQL plugin
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Disable Prisma telemetry
CHECKPOINT_DISABLE=1
